class Chune {
  String preview;
  String spotifyUri;

  Chune(
      {this.albumArt,
      this.songName,
      this.artistName,
      this.isSelected,
      this.preview,
      this.spotifyUri});

  var albumArt;
  var songName;
  var artistName;
  bool isSelected;
}
